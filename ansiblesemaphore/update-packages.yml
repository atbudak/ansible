---
- name: Update Packages
  become: yes
  hosts: lotrun01
  gather_facts: false
  tasks:
    # update for ubuntu
    - name: updating shell
      apt:
        update_cache: yes
        force_apt_get: yes
        cache_valid_time: 3600
