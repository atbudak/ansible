  - name: Connection Verify
    # hosts: "{{ pinghost }}"
    hosts: all
    gather_facts: false
    tasks:
      # update for ubuntu
      - name: ping
        ping:
#        with_items:
#          - lotrun01
      
